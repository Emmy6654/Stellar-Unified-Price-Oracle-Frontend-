name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run typecheck

      - run: npm run lint

      - run: npm run test:run

      - run: npm run build:analyze

      # Structural budgets: coverage, initial-load boundary, vendor attribution.
      # budgets.config.json is the source of truth; size-limit below is a
      # redundant backstop over the same initial-load set.
      - run: npm run check:budgets

      - run: npm run size-limit

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bundle-report
          path: reports/

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: build
          path: dist/
